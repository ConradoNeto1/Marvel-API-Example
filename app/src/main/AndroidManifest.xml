<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.eti.wagnermessias.marvelexample">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".characters.CharactersActivity" />
        <activity android:name=".dashboard.DashboardActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".comics.ComicsActivity" />
        <activity android:name=".events.EventsActivity" />
        <activity android:name=".series.SeriesActivity" />
        <activity android:name=".stories.StoriesActivity" />
        <activity android:name=".characters.DetalheCharacterActivity"></activity>
    </application>

</manifest>